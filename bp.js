const axios = require('axios');

const API_KEY = '';
const API_URL = ''

async function generateBusinessPlan() {
    try {
        const prompt = 'Write a business for a new startup in the business space ';
        const maxTokens = 1024;
        const response = await axios.post(API_URL, )
    }
}